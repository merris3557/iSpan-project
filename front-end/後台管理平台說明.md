# 後台管理平台使用說明

## 🎯 功能概述

這是一個現代化的後台管理平台，具有以下特點：

- ✅ 響應式側邊欄導航
- ✅ 多層級菜單結構
- ✅ 使用 custom.scss 自定義樣式
- ✅ 現代化的 UI 設計
- ✅ Bootstrap Icons 圖標支持

## 📁 文件結構

```
front-end/
├── src/
│   ├── layouts/
│   │   └── admin.vue          # 後台主佈局（含側邊欄）
│   ├── views/
│   │   ├── AdminDashboard.vue # 管理首頁（儀表板）
│   │   └── ERPTransferView.vue # ERP格式轉換頁面
│   ├── assets/
│   │   └── styles/
│   │       └── custom.scss    # 自定義樣式（已擴展）
│   └── router/
│       └── index.js           # 路由配置
```

## 🚀 訪問方式

訪問後台管理平台：`http://localhost:5173/admin`

## 📋 主要頁面

### 1. 後台首頁 (ERP格式轉換)
- 路徑: `/admin`
- 功能: ERP格式轉換工具
- 特點: 
  - 平台選擇下拉菜單
  - 文件上傳功能
  - 數據表格展示

### 2. 其他管理頁面
所有其他管理頁面路由已配置，目前使用相同組件作為佔位符：

- `/admin/frontend/banners` - 輪播圖管理
- `/admin/frontend/content` - 內容管理
- `/admin/news/list` - 消息列表
- `/admin/news/create` - 新增消息
- `/admin/knowledge/list` - 知識列表
- `/admin/knowledge/categories` - 分類管理
- `/admin/products/list` - 商品列表
- `/admin/products/create` - 新增商品
- `/admin/products/categories` - 商品分類
- `/admin/sales/orders` - 訂單管理
- `/admin/sales/reports` - 銷售報表

## 🎨 自定義樣式

`custom.scss` 已擴展包含：

### 顏色變量
- `$admin-primary`: #1e3c72 (主題藍色)
- `$admin-primary-dark`: #2a5298 (深藍色)
- `$admin-content-bg`: #f5f6fa (內容背景)

### 實用類
- `.bg-admin-primary` - 主題背景色
- `.text-admin-primary` - 主題文字色
- `.admin-card` - 卡片樣式
- `.btn-admin-primary` - 主要按鈕
- `.btn-admin-outline` - 外框按鈕
- `.admin-form-control` - 表單控件
- `.admin-table` - 表格樣式

## 🎯 側邊欄功能

- **收縮展開**: 點擊底部按鈕或頂部菜單圖標
- **多級菜單**: 支持子菜單展開/收起
- **路由高亮**: 當前頁面自動高亮
- **響應式**: 移動端自動適配

## 🛠 技術棧

- **Vue 3** - 前端框架
- **Vue Router** - 路由管理
- **SCSS** - 樣式預處理器
- **Bootstrap Icons** - 圖標庫
- **Vite** - 構建工具

## 📝 開發說明

### 添加新頁面

1. 在 `src/views/` 創建新組件
2. 在 `src/router/index.js` 添加路由
3. 在側邊欄 `src/layouts/admin.vue` 添加菜單項

### 自定義樣式

在 `custom.scss` 中添加變量和樣式類，然後在組件中使用：

```scss
@import '@/assets/styles/custom.scss';
```

## 🎨 設計特點

- **現代化漸變**: 使用漸變背景增強視覺效果
- **平滑過渡**: 所有交互都有流暢的動畫效果
- **卡片陰影**: 懸停時提升卡片，增強互動感
- **狀態徽章**: 彩色狀態標籤，清晰易讀
- **響應式設計**: 完美適配各種屏幕尺寸

## 🔧 進一步開發

您可以：

1. 為每個佔位符頁面創建實際內容
2. 添加 API 集成
3. 實現用戶認證
4. 添加更多圖表和數據可視化
5. 實現完整的 CRUD 功能

## 📱 瀏覽器支持

- Chrome (推薦)
- Firefox
- Safari
- Edge

---

**注意**: 確保已安裝所有依賴並運行 `npm install`，然後使用 `npm run dev` 啟動開發服務器。
